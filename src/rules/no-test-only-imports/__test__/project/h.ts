// Imported only by a test file — exempt because of the `_testOnly` prefix
export const _testOnlyHelper = 10;

// Not imported anywhere — also exempt because of the `_testOnly` prefix
export const _testOnlyUnusedHelper = 10;
